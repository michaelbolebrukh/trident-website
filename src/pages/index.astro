---
import { seoFor } from '../data/seo'
import BaseLayout from '../layouts/BaseLayout.astro'
import HomePage from '../components/HomePage'

const meta = seoFor('/', 'Trident Modular', 'British modular housing — garden rooms, bungalows and frame-built homes, managed from first design through to installation. Browse the range or request a quote.')
---

<BaseLayout
  title={meta.title}
  description={meta.description}
>
  <HomePage client:load />
</BaseLayout>
